# Production — copy to .env on the VPS: cp .env.production.example .env

POSTGRES_USER=pstn
POSTGRES_PASSWORD=change-me-use-a-strong-password
POSTGRES_DB=pstn

DATABASE_URL=postgresql://pstn:change-me-use-a-strong-password@postgres:5432/pstn

APP_PORT=5555
LOG_LEVEL=info
DB_POOL_MAX=10
DB_IMPORT_POOL_MAX=4

# Optional — see portainer.env.example
# IMPORT_SECRET=
